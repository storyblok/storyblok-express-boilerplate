{{#each story.content.header}}
  {{> (lookup . 'component') }}
{{/each}}

{{#each story.content.body}}
  {{> (lookup . 'component') }}
{{/each}}

{{#each story.content.footer}}
  {{> (lookup . 'component') }}
{{/each}}